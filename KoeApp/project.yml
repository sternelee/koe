name: Koe
options:
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"
  defaultConfig: Release

packages:
  KoeMLX:
    path: ../Packages/KoeMLX
  KoeAppleSpeech:
    path: ../Packages/KoeAppleSpeech
  KoeAppleTranslation:
    path: ../Packages/KoeAppleTranslation

settings:
  base:
    PRODUCT_BUNDLE_IDENTIFIER: nz.owo.koe
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    ENABLE_HARDENED_RUNTIME: YES
    APP_UPDATE_FEED_URL: "https://raw.githubusercontent.com/missuo/koe/main/docs/update-feed.json"

targetTemplates:
  KoeApp:
    type: application
    platform: macOS
    sources:
      - path: Koe
        excludes:
          - "*.entitlements"
          - "**/*Tests.m"
    settings:
      base:
        INFOPLIST_FILE: Koe/Info.plist
        CODE_SIGN_ENTITLEMENTS: Koe/Koe.entitlements
        PRODUCT_NAME: Koe
        HEADER_SEARCH_PATHS:
          - "$(inherited)"
          - "$(SRCROOT)/../koe-core/target"
        LIBRARY_SEARCH_PATHS:
          - "$(inherited)"
          - "$(SRCROOT)/../target/release"
        OTHER_LDFLAGS:
          - "-lkoe_core"
          - "-lc++"
          - "-lSystem"
          - "-lresolv"
          - "-framework Security"
          - "-framework CoreFoundation"
          - "-framework UserNotifications"
          - "-framework SystemConfiguration"
        ENABLE_APP_SANDBOX: NO
        CLANG_ENABLE_OBJC_ARC: YES
    frameworks:
      - AVFoundation
      - AppKit
      - ApplicationServices
      - Carbon
      - CoreAudio
      - AudioToolbox
      - ServiceManagement
      - Speech
      - UserNotifications
      - sdk: libsqlite3.tbd

targets:
  Koe:
    templates:
      - KoeApp
    settings:
      base:
        SWIFT_VERSION: "5.9"
    dependencies:
      - package: KoeMLX
        product: KoeMLX
      - package: KoeAppleSpeech
        product: KoeAppleSpeech
      - package: KoeAppleTranslation
        product: KoeAppleTranslation
      - target: KoeVirtualMic
        embed: true
    preBuildScripts:
      - name: Build Rust Library
        script: |
          set -euo pipefail
          export PATH="$HOME/.cargo/bin:/opt/homebrew/bin:/usr/local/bin:$PATH"
          command -v cmake >/dev/null || { echo "error: cmake is required to build Rust dependency audiopus_sys. Install it with: brew install cmake" >&2; exit 1; }
          cd "${SRCROOT}/.."
          RUST_TARGET="aarch64-apple-darwin"
          cargo build --manifest-path koe-core/Cargo.toml --release --target "$RUST_TARGET"
          mkdir -p "${SRCROOT}/../target/release"
          ln -sf "${SRCROOT}/../target/${RUST_TARGET}/release/libkoe_core.a" "${SRCROOT}/../target/release/libkoe_core.a"
        basedOnDependencyAnalysis: false

  Koe-lite:
    templates:
      - KoeApp
    settings:
      base:
        SWIFT_VERSION: "5.9"
    dependencies:
      - package: KoeAppleSpeech
        product: KoeAppleSpeech
      - package: KoeAppleTranslation
        product: KoeAppleTranslation
    preBuildScripts:
      - name: Build Rust Library
        script: |
          set -euo pipefail
          export PATH="$HOME/.cargo/bin:/opt/homebrew/bin:/usr/local/bin:$PATH"
          command -v cmake >/dev/null || { echo "error: cmake is required to build Rust dependency audiopus_sys. Install it with: brew install cmake" >&2; exit 1; }
          cd "${SRCROOT}/.."
          RUST_TARGET="aarch64-apple-darwin"
          cargo build --manifest-path koe-core/Cargo.toml --release --target "$RUST_TARGET" --no-default-features --features "apple-speech"
          mkdir -p "${SRCROOT}/../target/release"
          ln -sf "${SRCROOT}/../target/${RUST_TARGET}/release/libkoe_core.a" "${SRCROOT}/../target/release/libkoe_core.a"
        basedOnDependencyAnalysis: false

  Koe-x86:
    templates:
      - KoeApp
    dependencies:
      - package: KoeAppleSpeech
        product: KoeAppleSpeech
      - package: KoeAppleTranslation
        product: KoeAppleTranslation
    preBuildScripts:
      - name: Build Rust Library
        script: |
          set -euo pipefail
          export PATH="$HOME/.cargo/bin:/opt/homebrew/bin:/usr/local/bin:$PATH"
          command -v cmake >/dev/null || { echo "error: cmake is required to build Rust dependency audiopus_sys. Install it with: brew install cmake" >&2; exit 1; }
          cd "${SRCROOT}/.."
          RUST_TARGET="x86_64-apple-darwin"
          export CMAKE_POLICY_VERSION_MINIMUM=3.5
          cargo build --manifest-path koe-core/Cargo.toml --release --target "$RUST_TARGET" --no-default-features --features "sherpa-onnx,apple-speech"
          mkdir -p "${SRCROOT}/../target/release"
          ln -sf "${SRCROOT}/../target/${RUST_TARGET}/release/libkoe_core.a" "${SRCROOT}/../target/release/libkoe_core.a"
        basedOnDependencyAnalysis: false

  KoeLogicTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Koe/AppDelegate/SPAppDelegateLogicTests.m
      - path: Koe/Hotkey/SPHotkeyMonitor.m
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: nz.owo.koe.logic-tests
        CLANG_ENABLE_OBJC_ARC: YES
    frameworks:
      - XCTest
      - Foundation

  KoeVirtualMic:
    type: bundle
    platform: macOS
    sources:
      - path: VirtualMicPlugin/Sources
    settings:
      base:
        PRODUCT_NAME: KoeVirtualMic
        PRODUCT_BUNDLE_IDENTIFIER: nz.owo.koe.virtual-mic-driver
        WRAPPER_EXTENSION: driver
        INFOPLIST_FILE: VirtualMicPlugin/Resources/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        GCC_PREFIX_HEADER: ""
        HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/VirtualMicPlugin/Sources"
        OTHER_LDFLAGS:
          - "-framework CoreAudio"
          - "-framework CoreFoundation"
          - "-lc++"
    frameworks:
      - CoreAudio
      - CoreFoundation
